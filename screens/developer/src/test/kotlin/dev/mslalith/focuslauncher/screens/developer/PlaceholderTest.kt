package dev.mslalith.focuslauncher.screens.developer

import org.junit.Test

class PlaceholderTest {
    @Test
    fun `i am a placeholder test`() {
        // This is a placeholder test to make the CI happy.
        // The :screens:developer module has testing dependencies, but no actual tests,
        // which causes the build to fail on CI.
        // Feel free to remove me when you add real tests.
        assert(true)
    }
}
